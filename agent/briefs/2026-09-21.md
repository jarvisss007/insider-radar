# Insider Radar — 2026-09-21 [insider]

**Council: applied** — directive insider-radar 2026-09-17. Both KEEPs carried: every price
written today is disclosed as a live call-date bar rather than passed off as a close, and the
weekend `check_date` roll runs at write time. The FIX is discharged: **UBER is no longer open
and was not resolved early by me** — it carries an outcome from a later run. **INS-019 stands
open and unruled, and I did not resolve anything under my own reading of it:** PMTS asks about
the 2026-09-21 close and was DEFERRED, not read off today's live bar.

**Feed state:** 400 purchases, **40 clusters live**, 13 excluded; `updated_utc` 2026-09-21
15:29 UTC (~24 min old at write time). Collector exited clean, +0 new purchases this pass.

**New clusters logged (4):** BORR 2 insiders $5.16M · TH 2 insiders $375k · RVSB 5 insiders
$51k · KWY 2 insiders $4.1k. STELLUS PRIVATE CREDIT BDC (CIK 1901037, 3 insiders, $77k) was
not logged — standing disclosed exclusion, no price series (INS-007). **Form 4s lag the trade
by up to 2 business days, so the insiders' entry price is not our entry price.**

**Stale-quote line:** all four logged today carry `stale_quote = no`; none is `yes`, none is
empty. Decided at call time by `stale_quote.py --check`, not by eye.

**Scorecard.** Scored today: **1** (SNBH, 0.35 → 0.36, right). Never blended:
- **HEADLINE ≥$100k: 55% · n=60 on 14 entry days** (126 logged)
- **sub-floor <$100k: 50% · n=22 on 10 entry days** (72 logged) — proves nothing yet
- unknown: 0% · n=1
Past 20 scored on the headline stratum, so the bar applies: **55% on 60 rows but only 14
entry days is not distinguishable from a coin flip**, and the bar is not the coin — **SPY
returned +0.97% over the trailing ~30 calendar days**, which a long-only book collects for
free. The registered verdict is unchanged: no established insider edge (669 events, median
abnormal −0.010%, week-clustered t=1.31, bootstrap CI contains zero).

**Deferrals — the number that matters today. 15 ledger rows carry `check_date` 2026-09-21
and were NOT scored:** REFI, LIEN, UTGN, LBGJ, BRLT, PHIO, UMH, IAUX, INV, BGDE, NTHI, AIAI,
BY, RCG, MLCI. `sessions.settled_session()` returns **2026-09-18**; the 09-21 bars Yahoo is
serving are live intraday prints, and scoring 15 rows off them would have been §9 at the
largest scale this ledger has been offered. They resolve on the next run. Open rows: **109**.

**Firm Brain — §11, a defect this lab had not checked itself for.** §11 says a deferred row
looks open and any writer freezing on `outcome` alone will rewrite it. Audited this run: the
ledger's only writers are `append_call()` (append-only), `attribution.py` (writes
`attribution.csv`, not the ledger) and `strata.py` (read-only). **No writer re-derives an
existing row from a live source, so today's 15 deferrals are safe — but safe by accident**,
through the write path being append-only, not through any freeze key. Reported, not patched.

**Forecast filed:** `BORR · 5d · |5-session move| from 4.435 exceeds 10.0% on 2026-09-28 · p=0.15`.
Base rate 0.234 over 495 sessions of BORR's own tape, 0.107 over the trailing 6 months; 0.15
sits between, weighted toward the current vol regime, both numbers stated. No bin is
`actionable` (largest is 0.5–0.6 at n=17), so no adjustment made or permitted — though the
0.4–0.5 bin running 0.111 on n=9 is a standing warning against this book's habit of filing
near-coin-flips. **CAL-001:** `calibrate.py insider-radar 0.15` → 0.15, **[p_cal=0.15]**, unchanged.
Forecast resolved this run: **HELP → 0** (09-18 close 12.37 vs 09-14 close 13.61).

**Denominator, said out loud as the council asked:** 109 open rows against AGENT.md's bar of
20 scored calls — **met on rows, nowhere near met on days.** And tomorrow makes it worse, not
better: the 15 rows coming due were logged on 08-20 and 08-21, **two entry days**, so a hit
rate that moves 25% overnight will have moved on two days of information (§4).
