# Insider Radar — 2026-08-31 (Mon)

## Feed state
Collector clean: +2 purchases, feed 400, clusters 30, excluded 7. `updated_utc`
2026-08-31 15:30 UTC — 5 minutes old at write time. Study re-run: 961 events (669 mature),
verdict unchanged — **NO significant edge at any horizon** after market adjustment and
cluster-robust inference.

## Two rows are due today and are NOT scored — the session has not closed
SCTX and ACI both carry `check_date 2026-08-31`. This run fired 08:30 PDT; the US session
opened 06:30 PDT and closes 13:00 PDT. Yahoo already returns an **08-31 bar** for both
(SCTX 29.92, ACI 12.535) and it is a **live mid-session print, not a close**. Scoring off it
is precisely the `ASIA-005` defect the council ordered kept and not relaxed: a guard that
checks the clock has not checked the data (Firm Brain §1).

Both are deferred to the next run, which will see the settled 08-31 close. **Stated in
advance, before the outcomes are final, so this cannot be read as dodging:** on the current
live prints both would score `right` and comfortably — SCTX 29.92 vs a 20.40 anchor
(+46.7%) and ACI 12.535 vs 11.60 (+8.1%). Deferring costs this lab two wins today. That is
the correct direction to err.

**Audited the whole book against this, because deferring is only defensible if the existing
rows are clean.** All 12 scored rows were re-fetched and every recorded `price_at_check`
matches the settled close for its resolution bar **to the cent, 12/12** (the seven 08-16
rows correctly resolved on the 08-17 bar per the `INS-012` roll-forward ruling; the five
08-24 rows on the 08-24 close). The book has no mid-session contamination in it today, and
the deferral is what keeps it that way.

**This is an unruled class, and it is not `INS-012`.** `INS-012` ruled the case where the
check date has *no* bar (splits, halts, holiday holes) → roll forward to the next session
with a bar, stamped. Today's case is the opposite shape: **the bar exists and is not yet
final.** Nothing has ruled on it, and it is not rare — this sweep fires at 08:20 PT, which
is mid-session, so **every US check date this lab ever writes will first come due during an
open session.** See lessons for the proposed guard; nothing is shipped here.

## New clusters logged today — 5
| ticker | insiders | value | stratum | anchor |
|---|---|---|---|---|
| BLX | 2 | $178,020 | headline | 54.54 |
| BATL | 2 | $110,032 | headline | 1.31 |
| CBU | 2 | $50,277 | sub-floor | 63.16 |
| BIVI | 4 | $49,118 | sub-floor | 2.16 |
| TNXP | 2 | $40,180 | sub-floor | 13.59 |

All five `long`, `check_date 2026-09-30`, `tags fund`, `stale_quote = no` (5/5 clean; none
flagged, none empty). Form 4s lag the trade by up to 2 business days — BLX's second buy was
transacted 08-28 and filed 08-31, TNXP's 08-27 filed 08-31 — so **the insiders' entry price
is not our entry price**: BLX insiders paid 53.00 and 55.00, we anchor at 54.54.

**Anchor disclosure — changed today, and it is a correction, not a new convention.**
`price_at_call` for all five is the **2026-08-28 settled close**, not a mid-session print.
AGENT.md says "latest daily close"; a partial intraday bar is not a daily close, so this is
the faithful reading and the previous runs' mid-session anchors were the deviation. Evidence
it matters: last run logged MAIR at 26.39 while MAIR's actual 08-28 close was 26.33, and
today the same endpoint returns `None` for UNB's 08-31 bar — an illiquid name simply has no
mid-session print to anchor to. **Prior rows are NOT rewritten (BENCH-002).**

**One cluster refused:** FUNDRISE REAL ESTATE INTERVAL FUND (CIK 1777677), 3 insiders,
$1,377 — CIK-only, no price series, so it can never score. `INS-007`: not logged, already
auto-upserted to `agent/exclusions.csv` (first_seen 08-12, 11 sessions).

## Scorecard — never blended, and never without its benchmark
- **HEADLINE (≥$100k): 80% hit, n=10 on 2 entry days** · vs-SPY excess **+0.21 pp**
- **sub-floor (<$100k): 50% hit, n=2 on 1 entry day** · vs-SPY excess **−3.37 pp**
- **All 12 scored rows: 75% hit — and −0.38 pp behind SPY** (mean call +3.30% vs SPY
  +3.68%; beat-SPY rate 58%). 108 open, 0 due-and-scorable.

**The minus sign is the headline, not the 80%.** Both entry cohorts sat in a rising tape.
And read `n` honestly (Firm Brain §4): 12 rows are **2 entry days**. `n_events = 2`. Nothing
here supports or refutes anything.

*Note on the count: the 08-28 brief said 13 scored rows / 11bp behind; today `strata.py` and
`attribution.py` independently agree on **12**, and the excess recomputes to −38bp. Not
rewritten, not reconciled by hand — reported as computed, with the method stated (first SPY
close on or after each row's call and check date).*

## Council directive — proposal: make the benchmark travel with the hit rate
Asked 08-28: propose how vs-SPX travels with the hit rate everywhere it appears. Proposal,
**not shipped** — the census schema is not this lab's to change:

> **One formatter, one call site.** Add `strata.fmt_rate(rows) -> "80% · +0.21pp vs SPY ·
> n=10 / 2 days"` and make it the *only* way any surface renders a hit rate — brief header,
> dashboard card, census row. Not a second column added beside the first: a single string
> that is structurally incapable of omitting the benchmark, because there is no code path
> that produces the bare percentage. Firm Brain §6 — the reason the numbers drift apart is
> that three readers format independently; the guard is to leave only one reader.
> Machine-checkable test: `no_bare_hit_rate` — grep every published surface for a `%` that
> is not immediately followed by the ` · ` benchmark token; fails if any exists.

## Flagged for ruling — a self-contradictory open forecast row
`agent/forecasts.csv` row 2026-08-28 MAIR: the **question text says "on 2026-09-04"** while
the **`check_date` column says 2026-09-08.** Two readers of one row, disagreeing (Firm Brain
§6). Not edited — but note the window to fix it cheaply is *now*, before either date passes,
which is the only time the choice cannot be a choice about an outcome.

## Forecast (mandatory)
Filed: **BLX closes above 54.54 (its 08-28 settled close) on 2026-09-08, p = 0.51.**
Deliberately near coin-flip: the standing verdict is no established edge, and BLX is already
below the anchor intraday. Calibration checked — 0.50–0.60 bin n=6, said 0.512, happened
0.333, `actionable: false`, no adjustment permitted. Recorded plainly: this lab's Brier
skill is **−0.42 on n=9**, i.e. worse than the base rate. The rule's remedy is more
observations, not a hand-applied haircut, and no haircut was applied.

**Resolved today: none.** ONON (`check_date 2026-08-31`) is deferred for the same
unsettled-session reason as the ledger rows above.

Slow, statistical signal — not a trade trigger. Not advice.
