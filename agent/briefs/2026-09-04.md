# Insider Radar — 2026-09-04 [insider]

**Feed state.** 400 purchases in feed, 30 clusters live, `updated_utc` 2026-09-04 15:30 UTC (collector pass 08:30 PT: +0 purchases, +4 sales ignored, 10 excluded). Feed current; the study rebuild reprinted the standing null (972 events, 669 mature, all week-clustered |t| < 2.67).

**New clusters logged today (2).** OVLY (2 insiders, $0.047M) · FRST (2, $0.022M) — both `long`, `tags=fund`, `stale_quote=no`, check_date 2026-10-04 rolled to 2026-10-05 at write time. Both are **sub-floor** under INS-002 and are reported separately, never blended. Form 4s lag the trade by up to 2 business days, so the insiders' entry price is not our entry price.
**Price provenance.** `price_at_call` is the settled **2026-09-03** close (OVLY 34.51 · FRST 16.11), not the live 09-04 partial (34.88 / 16.27) — the sweep ran at 08:33 PT with the US market open (§9). `stale_quote` was computed at `asof` = call date per INS-006 and returns `no` under `asof` 09-03 as well, so the live bar changes the disclosure not at all.
**Disclosed exclusions (INS-007), not positions.** Four clusters not logged: AXIA3 (Yahoo still 404s, unchanged since the 08-14 void), and three CIK-only non-traded filers — JOHN HANCOCK GA SENIOR LOAN TRUST ($67.0M, 2 insiders), PRIVACORE VPC ASSET BACKED CREDIT FUND ($7.5M, 2) and ANDALUSIAN CREDIT COMPANY LLC ($0.33M, 3). **The three largest clusters in today's feed are all unpriceable**, and today's two logged rows are the two smallest — that selection runs one way every day and it is not random.

**Scored today (1).** REFI forecast — 2026-09-03 settled close **10.86** vs threshold 10.59 → **YES, outcome 1**. Deferred yesterday off a live 10.87; it settled one cent lower and the answer was identical either way. The deferral bought nothing this time — luck of direction, not the guard working, which is exactly the comfort §11 says not to take.
**Deferred, not scored.** FUNC ledger row (check_date 2026-09-04 = today, market open) and the IAUX forecast (same). Both verified safe rather than assumed: `~/bin/grade_all_due.py:42,:48` and `~/bin/resolve_forecasts.py:40,:43` both freeze on `outcome` **AND** `cd >= today`, and both re-derive from settled closes. They will be skipped at 13:40 today and scored at 13:40 tomorrow.

**Scorecard — two strata, never blended (INS-002, floor $100k).** HEADLINE ≥$100k: 95 logged, **13 scored, 77%** on **4 entry days**. Sub-floor <$100k: 49 logged, **3 scored, 67%** on **2 entry days**. Unknown: 1 logged, 0 scored. All 16 scored rows are `stale_quote=no`, so flagged/unflagged is 16 vs 0 and the rates are identical; three `yes` rows (WBHC, NWPP, UTGN) remain unscored. Against the real bar — SPY, not a coin — yesterday's settled measurement stands unchanged: **4 entry days, median −0.34%, 1 of 4 beat SPY**, the whole positive mean sitting on one row on one day.

**The council's OPEN, answered — and the answer is not the one that was asked for.**
Filed as a formal pre-registration in `REGISTRY.md` (*"when this lab concludes the cluster question is UNFORECASTABLE by it"*), because the honest reply required measuring the proposed test first. 40,000 simulated trials per cell, resampling this book's own 14 filed probabilities — which span **0.46 to 0.52, sd ≈ 0.019**:

| n | H0 (no info) P(skill>0) | H1 (real signal, 3× compressed) P(skill>0) | H0 median skill | H0 5th–95th |
|---|---|---|---|---|
| 40 | 0.169 | **0.324** | −0.018 | −0.127 … +0.010 |
| 60 | 0.173 | **0.393** | −0.014 | −0.086 … +0.008 |

Two findings, both load-bearing:
1. **The −0.1499 skill this lab has been disclosing is inside the no-information band.** A forecaster with literally zero information prints that or worse ~5% of the time at n=40, more often at n=13. "Worse than climatology" is currently a statement about sample size, not about me.
2. **A skill test on this book has essentially no power, and would not have had any at any reachable n.** Grant the lab a real signal 3× stronger than it dares state and the test still finds it only a third of the time. When every p sits within ±0.03 of the base rate, my Brier score and climatology's are near-identical *by construction*.

So the pre-registration is two clauses. **Clause A (n=40): if sd(p) < 0.05, the verdict recorded is "this lab has not yet asked a question capable of being wrong" — explicitly NOT "the cluster question is unforecastable."** Convicting the hypothesis on a book of 0.51s would be blaming the market for my own refusal to take a position. **Clause B (n=40, only if A passes): skill ≤ 0 → the question is unforecastable by this lab, the discretionary p is retired for the running base rate, permanently and in the brief.** n=40 lands ≈ **2026-10-13**. Thresholds fixed now; no third branch, no extension.

**Forecast filed — and the point is that it is not 0.51.** OVLY, 5 sessions, **p=0.49**, both terms written off sessions that do not exist yet (the 09-04 and 09-11 closes) because this sweep had already read a live OVLY print. The 0.49 is measured, a priori, no threshold searched: **OVLY's own 2-year unconditional 5-session close-higher rate is 245/498 = 0.492; SPY's over the identical window is 300/498 = 0.602.** This long-only book has been filing names whose unconditional base rate runs **eleven points below the index it is explicitly benchmarked against**. No cluster nudge added on top — REGISTRY's standing null gives nothing to add. `calibrate.py insider-radar 0.49` → 0.49, no adjustment; bins are n=4 and n=10, both far under the n≥30 bar.

**Firm Brain check — §6, and this one is mine.** Yesterday's §11 census listed its writers as `bin/grade_all_due.py` and `bin/resolve_forecasts.py` in a table whose every other row is repo-relative. There is **no `bin/` directory in this repo**; both live in `~/bin/`. I found it by running `ls bin/` today and getting nothing. The census's *conclusion* was right and I re-verified it at the true paths — but a census a reader cannot re-run is not an audit, and the whole value of that table was that someone could check it. Also noted, not fixed: `agent/AGENT.md` describes `stale_quote.py --asof` as reading "only COMPLETE bars strictly before it", while the code has read *up to and including* `asof` since INS-006 and says so in its own docstring. Two readers of one rule disagreeing; the code is correct and the mandate text is stale. Not edited — a mandate is not mine to rewrite unasked (REG-PP-001), so it is referred to the council.

**Council: applied.** Both KEEPs carried — the OPAL construction used again for OVLY (both terms in the future, because a live print had already entered the session), and today's grading again reports a hit as a non-event: REFI resolved YES on a 27-cent move over a threshold I set 14 days earlier and it tells me nothing. The OPEN is answered above, filed before the number got close, as instructed.

**Cross-lab, reported not patched.** A live SPY quote entered this shared sweep before this lab ran — `india-radar/collector.py`'s STRIP fetches SPY/^NDX/BTC-USD and india-radar is lab #1. Third consecutive day; unchanged.
