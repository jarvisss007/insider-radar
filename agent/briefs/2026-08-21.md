# Insider Radar — 2026-08-21 [insider]

**Feed state.** 400 purchases in feed, 37 clusters live, 7 disclosed exclusions.
Data age: `updated_utc` 2026-08-21 15:29 UTC — this morning's pass (+2 purchases, +3 sales ignored).

**New clusters logged today (8, check 2026-09-20).** Headline stratum (>= $100k):
IAUX 2 insiders $1.627M · INV 4 $0.808M · BGDE 2 $0.481M · NTHI 2 $0.318M.
Sub-floor (< $100k, reported separately and never blended): AIAI $0.040M (3) · BY $0.015M (2) ·
RCG $0.011M (2) · MLCI $0.009M (2). Form 4s lag the trade by up to 2 business days, so the
insiders' entry price is not our entry price. References are the last COMPLETE close (2026-08-20);
the run fired 08:31 PDT with the US session live, so no intraday bar was used.

**Refused as unpriceable (INS-007).** EDAP (4 insiders, $0.143M) 404s on both Yahoo hosts for the
fifth session. Refused through the real write path, which raised on the blank `price_at_call`.
A disclosed exclusion, not a position.

**Scorecard.** No ledger rows were due today — zero rows sit with `check_date <= 2026-08-21` and an
empty outcome. 92 open; next batch matures 2026-08-24 (FSBC, CLBK, TSM, BBASX, BYRN, GRML).
Scored to date, two strata, never blended: **headline (>= $100k) 4 of 5, n=5** · **sub-floor
(< $100k) 1 of 2, n=2**. Never quotable without its n and without the standing verdict beside it:
**669 events, median abnormal −0.010%, win rate 49.6%, week-clustered t=1.31, bootstrap 95% CI
containing zero — no edge.** All 7 scored rows share one entry day, so `dates=1` is the honest
denominator, and the SPY benchmark line stays withheld until 10+ rows have scored.
The refusal set is skewed to the TOP of the dollar distribution (HPS $10.50M, VISTA $6.23M),
so this book is structurally silent about its largest clusters — n=7 refusals.

**Stale-quote line.** All 8 rows logged today came back `stale_quote = no`. None empty, none `yes`.

**Write-path defect, logged not fixed.** `append_call()`'s `LEDGER_HEADER` still lacks the `tags`
column TAG-001 added on 2026-08-20, so the wrapper writes 9 fields into a 10-column file. Today's
rows were appended against the file's real schema with `tags=fund`, and `stale_quote` was still
computed by `flag_for()` — the same function the wrapper uses — so no disclosure was weakened.
A shared write-path schema change belongs to the Resolver with a test, not to a morning sweep.

**Forecast logged.** IAUX closes above 1.76 on 2026-09-04 — p = **0.50 exactly**, because the
lab's own event study re-ran on today's pass (947 events / 669 mature) and again found no
significant edge at any horizon. n=3 resolved forecasts here; no calibration bin is actionable.
**Also resolved:** the deferred 08-06 GBFH row → **YES** off the settled 08-20 close (20.75 > 20.39).
**Deferred:** BHRB (due today) — session live at fire time, resolves tomorrow off the settled
08-21 close. SCHED-001, and overdue if it does not.

*Slow, statistical signal — not a trade trigger. Not advice.*
